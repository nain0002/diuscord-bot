/**
 * Markers Client Module
 * Handles general markers and visual elements
 */

// Render loop for drawing text and effects
mp.events.add('render', () => {
    // This is where we can add custom rendering logic
});

console.log('[Client] Markers module loaded');
